(do
  (require 'portfolio)
  (use 'ring.adapter.jetty
       'compojure.core
       'ring.middleware.stacktrace
       'portfolio.web)
  (wrap! app :stacktrace)
  (future
    (run-jetty (var app) {:port 8080})))

